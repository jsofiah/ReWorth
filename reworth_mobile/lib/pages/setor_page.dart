import 'package:flutter/material.dart';

class SetorPage extends StatelessWidget {
  const SetorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Halaman Setor"),
      ),
      body: const Center(
        child: Text("Ini halaman Setor"),
      ),
    );
  }
}