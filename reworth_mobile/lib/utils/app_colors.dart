import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const Color primary = Color(0xFFBBDE2D);
  static const Color secondary = Color(0xFF74942B);
  static const Color accent = Color(0xFFECC520);
  static const Color lightAccent = Color(0xFFE6FF66);

  // Base
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);

  // Background
  static const Color background = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textHint = Color(0xFFAAAAAA);

  // Input
  static const Color inputBorder = Color(0xFFE0E0E0);
  static const Color inputIcon = Color(0xFFAAAAAA);

  // Checkbox
  static const Color checkboxBorder = Color(0xFFBBBBBB);

  // Divider
  static const Color divider = Color(0xFFE5E5E5);
}